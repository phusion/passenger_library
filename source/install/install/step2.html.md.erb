---
section: install
---
<%
@page_title = "Installing #{edition_title} #{integration_mode_name_as_passenger_suffix}"
content_for(:sidebar) { render_partial("../sidebar", locals: locals) }
%>

# Installing <%= edition_title %> <%= integration_mode_name_as_passenger_suffix %>

### Introduction

Passenger can be installed through a variety of installation methods. We recommend using an operating system-specific installation method because it has better OS integration, but you can also choose a more generic installation method.

#### Is this the right guide for you?

<%= render_partial("shared/intro", locals: locals) %>

<div class="info">
  Are you looking to install Passenger on Heroku? Please follow <a href="https://github.com/phusion/passenger-ruby-heroku-demo">the Heroku guide</a>.
</div>

<h2 id="select_os">Select operating system / installation method</h2>

To continue, please select an operating system or installation method.

<%= render_partial("shared/os_selector",
      locals: locals.merge(context: :install_passenger, next_page: "")) %>
