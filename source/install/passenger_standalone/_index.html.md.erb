---
title: Installing Passenger (open source edition) in production using its Standalone mode
---
<%
locals[:type] = "standalone"
locals[:enterprise] = false
-%>
# Installing Passenger (open source edition) in production using its Standalone mode

### Introduction

<div class="image-float-right hidden-xs">
  <p><img src="<%= url_for '/images/passenger.png' %>" alt="Passenger"></p>
</div>
<div class="visible-xs" style="text-align: center">
  <img src="<%= url_for '/images/passenger.png' %>" alt="Passenger">
</div>

<%= render_partial("../shared/intro", locals: locals) %>

This guide is NOT for Ruby apps!
: Are you deploying Ruby apps? Check out [the Ruby-specific Standalone installation guide](<%= url_for 'ruby.html' %>).

<h2 class="notoc">Select operating system</h2>

<%= render_partial("../shared/os_selector", locals: locals) %>

<div class="install_os install_os_any" style="display: none">
  <p><strong>Table of contents</strong></p>
  <ol class="toc-container"><li>Loading...</li></ol>
</div>

## Installation guide

<div class="install_os install_os_none" style="display: none">
  <p>Please select an operating system from the above dropdown.</p>
</div>
<div class="install_os install_os_any alert alert-warning" style="display: none">
  Remember, this installation guide is <em>not for Ruby apps</em>! Are you deploying Ruby apps? Check out <a href="<%= url_for 'ruby.html' %>">the Ruby-specific Standalone installation guide</a>.
</div>

<%= render_partial("../shared/install", locals: locals) %>

## Conclusion

<%= render_partial("../shared/conclusion", locals: locals) %>
