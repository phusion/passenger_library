---
section: deploy_walkthrough
subsection: launch_server
---
<% content_for(:sidebar) { render_partial("_toc.html", locals: locals) } %>
<%= render_partial("launch_server/#{infrastructure_type}.md", locals: locals) %>

<% spec = deployment_walkthrough_next_step_after_launching_server(locals) %>
<a href="<%= spec[:url] %>" class="btn btn-primary btn-large">Continue &raquo;</a>
