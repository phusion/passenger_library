---
section: deploy_walkthrough
subsection: deploy_app
---
<%
@page_title = "Deploying a #{language_name} app on #{infrastructure_name_with_determiner} production server with #{edition_long_title} in #{integration_mode_name} mode (part 2) - #{edition_title} + #{language_name} deployment tutorial"
content_for(:sidebar) { render_partial("_toc.html", locals: locals) }
%>

<h1>Deploying a <%= language_name %> app on <%= infrastructure_name_with_determiner %> production server<br>
<small>with <%= edition_title %> in <%= integration_mode_name %> mode</small></h1>

<% if language_type == :ruby && infrastructure_type == :heroku %>
  <%= render_partial("/walkthroughs/deploy/deploy_app/ruby_heroku", locals: locals) %>
  <a href="deploy_updates.html" class="btn btn-primary btn-lg">Continue: Deploying updates &raquo;</a>
<% elsif language_type == :ruby && infrastructure_type == :cloud66 %>
  <%= render_partial("/walkthroughs/deploy/deploy_app/ruby_cloud66", locals: locals) %>
  <a href="deploy_updates.html" class="btn btn-primary btn-lg">Continue: Deploying updates &raquo;</a>
<% elsif language_type == :ruby && integration_mode_type == :standalone %>
  <%= render_partial("/walkthroughs/deploy/deploy_app/intro", locals: locals) %>
  <%= render_partial("/walkthroughs/deploy/deploy_app/standalone", locals: locals) %>
  <a href="deploy_updates.html" class="btn btn-primary btn-lg">Continue: Deploying updates &raquo;</a>
<% else %>
  <p>
    Now that proper preparations have been made, it is time to deploy your app. :-)
  </p>
  <p>
    But first, can you tell us how Passenger was installed?
  </p>

  <%= render_partial("/install/install/shared/os_selector",
        locals: locals.merge(context: :deploy_app, next_page: "deploy_app.html")) %>
<% end %>
