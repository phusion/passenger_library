---
title: Deployment, scaling and high availability
comments: false
advanced_guides: true
---

<%
  locals = locals.merge(INTEGRATION_MODE_STANDALONE)
  integration_mode_type = locals[:integration_mode_type]
%>

<h1>Deployment, scaling and high availability</h1>
<%= render_partial('/shared/current_selection') %>

<p class="lead">These guides assume that Passenger is already installed.</p>

<div class="info">If you are not familiar with Passenger, it's best to follow the <a href="<%= url_for "/tutorials/quickstart/" %>">quickstart</a> or <a href="<%= url_for "/tutorials/deploy_to_production/" %>">deployment tutorial</a> first.</div>

<%= render_partial("../shared/_list_of_articles", locals: locals) %>
