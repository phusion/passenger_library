<% SUPPORTED_REDHAT_VERSIONS.keys.each do |el| %>
<div class=<%= el.inspect %>>
  <%= render_partial('/advanced_guides/install_and_upgrade/install_os_content/redhat', locals: locals.merge(no_back_button: true, os_config_type: el.to_sym, edition_title: edition_type == :enterprise ? "Passenger Enterprise" : "Passenger")) %>
</div>
<% end %>
